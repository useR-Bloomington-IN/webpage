baseurl             = "/"   # Your domain name. Must end with "/"
languageCode        = "en-us"
title               = "Bloomington R UseR Group"
theme = "hugo-theme-introduction"
enforce_ssl         = false
builddrafts         = false                             # Include pages with draft status of true
canonifyurls        = true                              # Turns relative urls into absolute urls
enableEmoji = true # emoji support
ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "_files$", "_cache$"]
# disqusshortname   = "xxx"                             # Enable Disqus for comments
# googleAnalytics   = "xxx"

[permalinks]
    post = "/:year/:month/:day/:slug/"

[params]
    bloghead            = "Posts"                        # Full name shows on blog post pages
    firstname           = "Bloomington R UseR Group"                # First name shows in introduction on main page
    tagline             = "Open to users of all levels"       # Appears after the introduction
    introheight         = "fullheight"                       # Input either 'medium' or 'large' or 'fullheight'
    themestyle          = "light"                        # Choose 'light' or 'dark'
    avatar              = "img/Rlogo.png"             # Path to image in static folder eg. img/avatar.png, or comment out to remove
    email               = "user.bloomington.in@gmail.com"         # Shows in contact section, or leave blank to omit
    localtime           = true                          # Show your current local time in contact section
    timezone            = "America/Indianapolis"         # Your timezone as in the TZ* column of this list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
    dateform            = "Jan 2, 2006"
    dateformfull        = "Mon Jan 2 2006 15:04:05 EST"
    cachebuster         = false                         # Add the current unix timestamp in query string for cache busting css assets (relevant in development mode)
    description         = "Local R User group for Bloomington, IN, USA"        # Max 160 characters show in search results
    faviconfile         = "img/fav.ico"
    pygmentsStyle       = "monokai"                     # Choose theme: https://help.farbox.com/pygments.html
    pygmentsCodeFences  = true
    footertext          = ""                            # Text to show in footer (overrides default text)
    fadein              = true                          # Turn on/off the fade-in effect

    showblog            = true                          # Show Blog section on home page
    showprojects        = false                          # Show Projects section on home page
    projectcolumns      = "3"                           # Choose "2" or "3" columns for projects section
    placeholderimg      = "/img/workday.jpg"            # For Projects without image
    showallposts        = false                         # Set 'true' to list all posts on home page, or set 'false' to link to separate blog list page
    showlatest          = true                          # Show latest blog post summary
    sharebuttons        = true                          # On post pages, show share this social buttons

    # Share buttons
    sharetwitter        = false
    sharefacebook       = false
    sharepinterest      = false
    sharegoogleplus     = false

# Social icons appear in introduction and contact section. Add as many more as you like.
# Find icon names here: http://fontawesome.io/cheatsheet/

[[params.social]]
    url = "https://github.com/useR-Bloomington-IN"
    icon = "github"
